// function
// function is a block of code that perform an action & is reuseble

function isEven(num) {
    if (num % 2 === 0) {
        return true;
    } else {
        return false;
    }
}

function isEven(num) {
    return num % 2 === 0;
}

// 1, function decleration.
function add(a, b) {
    return(a+b)
}

const sum = add(2, 4);
console.log(sum)

